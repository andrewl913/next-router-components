import { routeRegistry, withRouteComponents } from "./src/configure";
import { Route, TogglableRouteLink } from "./src";

export { 
    routeRegistry,
    withRouteComponents, 
    Route, 
    TogglableRouteLink 
}