import { routeRegistry, withRouteComponents } from "./src/configure";
import { Route, ToggableRouteLink } from "./src";

export { 
    routeRegistry,
    withRouteComponents, 
    Route, 
    ToggableRouteLink 
}